name: C/C++ CI

on: [push, pull_request]

jobs:
  build:
    runs-on: [ubuntu-latest, 	macos-latest, windows-latest]
    
    steps:
    - uses: actions/checkout@v1
    - name: Install Qt deps
      uses: jurplel/install-qt-action@v2
    - name: qmake
      run: qmake "CONFIG +=test"
    - name: make
      run: make
    - name: make check
      run: make check
